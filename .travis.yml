language: cpp
os: linux
dist: bionic

compiler:
  - gcc
  - clang
install:
  - sudo apt install libcurl4 libcurl4-openssl-dev
  - sudo apt update

script:
  - mkdir build
  - cd build
  - cmake ..
  - make